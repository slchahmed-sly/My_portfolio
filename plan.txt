Phase 1: Backend Implementation (Django)
Action Required: Update the Django backend to support model translation using django-modeltranslation.

Dependencies:

Install django-modeltranslation.

Add it to INSTALLED_APPS in settings.py (It must be added before django.contrib.admin).

Settings Configuration (settings.py):

Define LANGUAGES:

Python

LANGUAGES = [
    ('en', 'English'),
    ('fr', 'French'),
    ('ar', 'Arabic'),
]
Set MODELTRANSLATION_DEFAULT_LANGUAGE = 'en'.

Add 'django.middleware.locale.LocaleMiddleware' to MIDDLEWARE. (Must be after SessionMiddleware and before CommonMiddleware).

Translation Registration:

Create a new file base/translation.py.

Register the Project, Skill, and TimelineEvent models.

Specify which fields need translation (e.g., title, description, full_description for Projects).


Database Migration:Run makemigrations and migrate. (This should create columns like title_en, title_fr, title_ar).Admin Panel:Update base/admin.py to inherit from TranslationAdmin instead of standard admin.ModelAdmin.This will allow me to edit all 3 languages in the dashboard tabs.Phase 2: Frontend Implementation (React)Action Required:Implement react-i18next for static text and language switching logic.Dependencies:Install i18next, react-i18next, i18next-browser-languagedetector, i18next-http-backend.Translation Files:Create folder structure: src/locales/Create en.json, fr.json, ar.json.Initial Keys to add: Navbar links (Home, Projects, Blog), Section Headers (About Me, Skills), and Common Buttons (Read More, Contact).Configuration (src/i18n.js):Initialize i18next.Use LanguageDetector to remember user preference.Set fallback language to 'en'.Language Switcher Component:Create components/LanguageSwitcher.jsx.It should be a dropdown or row of buttons (EN | FR | AR).Crucial Logic: When language changes:Call i18n.changeLanguage().Check if language is 'ar'.If 'ar', set document.documentElement.dir = 'rtl' and document.documentElement.lang = 'ar'.If 'en'/'fr', set dir = 'ltr'.API Data Handling:Update the axios or fetch calls. When requesting data from Django, add the header:'Accept-Language': i18n.languageThis ensures Django sends back the correct language for dynamic content (Projects/Blogs).Phase 3: Tailwind RTL StylingAction Required:Refactor CSS to support RTL mirroring automatically.Logical Properties:Scan Navbar.jsx, Hero.jsx, and ProjectCard.jsx.Replace physical directions with logical directions:ml-* (margin-left) $\to$ ms-* (margin-start).mr-* (margin-right) $\to$ me-* (margin-end).pl-* $\to$ ps-*.pr-* $\to$ pe-*.text-left $\to$ text-start.text-right $\to$ text-end.Verification:Ensure the Timeline component (border-left) moves to the right side when in RTL mode.